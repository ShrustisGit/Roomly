
<nav class="navbar navbar-expand-lg bg-body-light border-bottom sticky-top ">
    <div class="container-fluid">
      <a class="navbar-brand d-flex align-items-center gap-2" href="/listings">
        <i class="fa-solid fa-registered fa-2xl" style="color: #fe424d;"></i>
        <span class="fw-bold fs-4 roomly">Roomly</span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse " id="navbarNavAltMarkup">
        
        <div class="tax-toggle ms-auto">
          <div class="form-check form-switch">
            <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault">
            <label class="form-check-label" for="flexSwitchCheckDefault">Deplay total after taxes</label>
          </div>
        </div>
        <div class="navbar-nav ms-auto">
         
          <a class="nav-link home" href="/listings"><b>Home</b></a>
          <% if(currentUser && currentUser.username === "ShrustiHalli"){ %>
            <a class="nav-link" href="/listings/new" ><b>Create New</a></b>
            <% } %>
            
          <!-- <% if(!currentUser){ %>
          <a class="nav-link active"  href="/signup"><b>Sign Up</b></a>
          <a class="nav-link" href="/login"><b>Log in</b></a>
          <% } %>
          <% if(currentUser){ %>
          <a class="nav-link" href="/logout"><b>Log Out</b></a>
          <% } %>
          
          <a class="nav-link" href="/reset-password"><b>reset </b></a> -->
          
          <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                <b>More Options</b>
            </a>
            <ul class="dropdown-menu">
              <% if(!currentUser){ %>
                <li><a class="dropdown-item" href="/signup">Sign Up</a></li>
                <li><a class="dropdown-item" href="/login">Log in</a></li>
                <% } %>
                <% if(currentUser){ %>
                <li><a class="dropdown-item" href="/logout">Log Out</a></li>
                <li><a class="dropdown-item" href="/resetlogin">Reset Password</a></li>
                <% if(currentUser && currentUser.username !== "ShrustiHalli"){ %>
                  <li><a class="dropdown-item" href="/listings/bookes"><b>Bookings</b></a></li>
                  <% } %>
                
                <% } %>
                
            </ul>
        </div>
        </div>
      </div>
    </div>
  </nav>

  
  